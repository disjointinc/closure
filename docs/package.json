{
  "name": "@closure/docs",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "mint dev --port ${CLOSURE_DOCS_PORT:-3236}",
    "start": "node sync-openapi.ts --once && mint dev --port ${CLOSURE_DOCS_PORT:-3236}",
    "validate": "mint validate",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "eslint",
    "lint:fix": "eslint --fix"
  },
  "devDependencies": {
    "mint": "^4.2.894",
    "vitest": "^4.1.11"
  }
}
